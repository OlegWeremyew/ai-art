{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "build preview": "nuxt build && yarn run preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@nuxt-modules/compression": "^0.1.0",
    "dotenv": "^16.2.0",
    "nuxt": "^3.5.2",
    "nuxt-swiper": "^1.1.0",
    "pinia": "^2.1.3"
  },
  "devDependencies": {
    "@averjs/nuxt-compression": "^1.2.2",
    "@pinia/nuxt": "^0.4.11",
    "@types/axios": "^0.14.0",
    "@types/node": "^18",
    "axios": "^1.4.0",
    "sass": "^1.62.1",
    "vite-plugin-compression": "^0.5.1"
  }
}
